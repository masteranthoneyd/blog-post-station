# Rust Runtime 安装

安装:

```
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

默认安装位置:

* `CARGO_HOME`: `~/.cargo`
* `RUSTUP_HOME`: `~/.rustup`

> 如果国内安装慢, 请使用 [***rsproxy***](https://rsproxy.cn/).